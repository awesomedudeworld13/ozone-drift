numpy>=2.0,<3
pandas>=2.2,<4
scikit-learn>=1.5,<2
scipy>=1.13,<2
joblib>=1.4,<2
requests>=2.32,<3
# The shared evaluation harness. Pinned to git until 0.1.0 is on PyPI; swap to
# 'benchgap>=0.1.0' once it is. Every domain in this study must score with the
# same code, which is the whole reason it is a package and not a copied file.
benchgap @ git+https://github.com/awesomedudeworld13/benchgap@main
